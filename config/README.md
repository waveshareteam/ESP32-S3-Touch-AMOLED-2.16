# Config

This directory is reserved for shared ESP-IDF configuration overlays.

Current examples keep their own `sdkconfig.defaults` files. Add shared overlays
here only when multiple first-party projects need the same maintained settings.

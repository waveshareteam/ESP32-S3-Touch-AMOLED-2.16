# Firmware

This directory contains factory flashing and recovery artifacts.

Current artifact:

- `ESP32-S3-Touch-AMOLED-2.16-FactoryOnly-260318.bin`

Factory binaries are checked in intentionally and are excluded from build CI.
Source-built ESP-IDF and Arduino firmware is generated by GitHub Actions and uploaded as
workflow artifacts instead of being committed to this directory.
